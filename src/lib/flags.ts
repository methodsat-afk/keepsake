/**
 * WAITLIST_MODE — flip to false once the product is ready to launch.
 * true  → landing page shows a waitlist email form instead of the paywall CTAs.
 * false → landing page shows the normal "Get started / $19" flow.
 */
export const WAITLIST_MODE = true;
